BOT_TOKEN=your-bot-token
# Optional for fast local iteration. If omitted, commands are deployed globally.
GUILD_ID=your-development-guild-id
# Set to false to skip automatic deployment on startup.
AUTO_DEPLOY_COMMANDS=true
