DISCORD_TOKEN=your_bot_token_here

# Note: CLIENT_ID and GUILD_ID are no longer required!
# The bot automatically detects its client ID after login,
# and commands are registered globally to work in all servers.
